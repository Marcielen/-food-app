import { BrowserRouter } from "react-router-dom";
import { RoutesContent } from "routes/index";

function App() {
  return (
    <BrowserRouter>
      <RoutesContent />
    </BrowserRouter>
  );
}

export default App;
